app.factory('DefaultvaluesFactory', function($http){
    var factory = {};           

    factory.get_work_days = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/get_work_days.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    };

    factory.get_birthday_leave = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/get_birthday_leave.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    };

    factory.get_work_hours = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/get_work_hours.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    };

    factory.get_default_values = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/get_default_values.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    };

    factory.get_leave_types = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/get_leave_types.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    };

    factory.get_leaves_filed = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/get_leaves_filed.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    };

    factory.fetch_types = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/cutoff_types.php',
            method: 'GET'
        })

        return promise;

    };

    factory.fetch_dates = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/fetch_dates.php',
            method: 'GET'
        })

        return promise;
    };

    factory.save = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/save_work_days.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    }; 

    factory.save_hrs = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/save_work_hrs.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    }; 

    factory.save_color = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/save_color.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    }; 

    factory.save_cutoff = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/save_work_cutoff.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    };

    factory.update_default_values = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/update_default_values.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    }; 

    factory.update_birthday_leave = function(data){
        var promise = $http({
            url:'./FUNCTIONS/Defaultvalues/update_birthday_leave.php',
            method: 'POST',
            headers: {'Content-Type': 'application/x-www-form-urlencoded'},
            transformRequest: function(obj) {
                var str = [];
                for(var p in obj)
                str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));
                return str.join("&");
            },
            data : data
        })

        return promise;
    }; 
    
    return factory;
});